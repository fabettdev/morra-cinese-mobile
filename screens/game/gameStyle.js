import { StyleSheet } from "react-native";

const gameStyle = StyleSheet.create({
  container: {
    position: "absolute",
    flexDirection: "row",
    justifyContent: "space-evenly",
    alignItems: "center",
    bottom: 0,
    height: "75%",
    width: "100%",
  },
});

export { gameStyle };
